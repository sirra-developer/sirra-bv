export type ServicesIntroContent = { heading: string; introduction: string };
export type ServiceAreaContent = {
  number: string;
  title: string;
  text: string;
};
export type ServicesScanContent = {
  eyebrow: string;
  heading: string;
  text: string;
  action: string;
  actionUrl: string;
};
